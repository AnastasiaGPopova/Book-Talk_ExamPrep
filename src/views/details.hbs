
    <section class="details-page">
        <h1>Details</h1>
        <article class="details-card">

            <article class="details-card-text">
                <h2>Title:<br> {{currentBook.title}}</h2>
                <h3>Author: {{currentBook.author}}</h3>
                <h3>Stars: {{currentBook.stars}}</h3>
                <h3>Genre: {{currentBook.genre}}</h3>
                <h3>Review: {{currentBook.bookReview}}
                </h3>

                {{#if isLogged}}
                <!-- If there is no registered user, do not display buttons-->
                {{#if isOwner}}
                <div class="buttons">
                    <!-- Only for registered user and author of the review -->
                    <a href="/{{currentBook._id}}/edit" class="btn-edit">Edit</a>
                    <a href="/{{currentBook._id}}/delete" class="btn-delete">Delete</a>
                {{else}}
                    {{#if isWishedAlready}}
                    <!-- logged in user who has already wished book-->
                    <p class="wish-pub">You already added the book to your wish list</p>
                    {{else}}
                    <!-- logged in user who has not yet wished book-->
                    <a href="/{{currentBook._id}}/wish" class="btn-wish">Wish to read</a>
                    {{/if}}
                {{/if}}
                </div>
                {{/if}}

            </article>

            <article class="details-card-image">
                <img src="{{currentBook.imageUrl}}">
            </article>

        </article>
    </section>
